# Art Portfolio Story

A single-page art portfolio with story-like vertical panels and animated transitions.

## Run
Open `index.html` in a browser.

## Customize
- Replace the Unsplash image URLs in `styles.css`.
- Update titles and descriptions in `index.html`.
